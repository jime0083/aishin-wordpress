====================================================================
problem.txt — 問題・修正の記録
====================================================================
運用ルール:
- 問題発生時に必ずここへ P番号（P-001〜）で記録してから修正に着手する
- フォーマット:
  ## P-XXX: 問題タイトル
  - 発生日: YYYY-MM-DD
  - 状態: 調査中 / 修正中 / 解決済み（解決日）
  - 現象: （どのページ・どの幅・どの操作で何が起きたか）
  - 原因: （調査で特定した根本原因）
  - 修正: （実施した修正内容と対象ファイル）
  - 確認: （React版との照合結果）

====================================================================
記録
====================================================================

## P-001: トップFVに黒い円形の回転バッジ（JOIN OUR TEAM）が表示される（React版にはない）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番反映＋スーパーリロードでユーザー確認済み。badge非表示）
- 現象: 本番トップページのファーストビュー右下に、黒い円形の回転するバッジ
        （円周テキスト「JOIN OUR TEAM ・ AISHIN INC. ・ ENTRY ・」＋針マーク）が
        表示される。React版（正）には表示されていない。全画面幅で発生。
- 原因: `.hero__badge` は home.css で `opacity: 0`（初期非表示）。JSアニメーションで
        opacity 1 にして表示する設計。
        React版 Hero.tsx はこのアニメーションを `gsap.context(fn, contentRef)` の
        スコープ内で登録しているが、`.hero__badge` は `hero__content`（=contentRef）の
        **外**（hero__main直下）にあるためセレクタにマッチせず、opacity:0 のまま＝
        非表示になっている（React版の実挙動）。
        一方 WordPress版 hero.js は document 全体から `.hero__badge` を取得して
        アニメーションするため opacity:1 になり表示されてしまう。
- 修正方針: hero.js から hero__badge のアニメーション（fromtoと reduced時のgsap.set）を
        削除し、CSS初期値 opacity:0 のまま非表示にする（React版の実挙動と一致）。
        マークアップ（hero.php の badge）はReact版にもDOMが存在するため残す。
- 対象: wp-content/themes/aishin/assets/js/hero.js

## P-002: 全ページのENTRYセクションで「ENTRY」文字が枠をはみ出す（中央に収まらない）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番でENTRY中央揃えをユーザー確認済み。loaderは.loader__brandがflexのため修正不要と確認）
- 現象: 全ページ共通の ENTRY CTA セクションで、巨大文字「ENTRY」が点線枠を左右に
        はみ出す。React版（正）はどの画面幅でも枠内に中央揃えで収まる。
- 原因: `.entry__char` は `display: inline-block`。inline-block 要素間の改行・空白は
        レンダリング時に半角スペースとして表示される。
        WordPress版 section-entry-cta.php は foreach で各 `<span>` を**改行して**
        出力しているため、E N T R Y の各文字間に空白が入り、`white-space:nowrap` の
        `.entry__title` の幅が広がって枠をはみ出す。
        React版（JSX）は要素間に空白が入らないため詰まって枠内に収まる。
- 修正方針: entry__char の span を改行せず**連続出力**（PHPで echo 連結）して
        文字間の空白を除去する。同じパターン（HTML直書きforeachでinline-block span）が
        他テンプレートにないか確認する（loader__brand-char 等）。
- 対象: wp-content/themes/aishin/template-parts/section-entry-cta.php
        （必要に応じて template-parts/loader.php も）

## P-003: インタビュー各Q&Aの見出し「QUESTION」を「REASON」に変更（ブラッシュアップ）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番反映確認済み）
- 現象: インタビュー詳細ページの各Q&Aセクション上部の eyebrow が「0N QUESTION」表示。
        これを「0N REASON」に変更したい（ユーザー要望・React版に縛られない改善）。
- 原因: single-interview.php の eyebrow に "QUESTION" が直書きされている。
- 修正方針: single-interview.php の "QUESTION" を "REASON" に変更。
- 対象: wp-content/themes/aishin/single-interview.php

## P-004: インタビューQ&Aの写真がレスポンシブ時に本文の下に来る→上に配置したい（ブラッシュアップ）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番反映確認済み）
- 現象: 900px以下のレスポンシブ表示で、各Q&Aの写真がセクションの最下部（本文の下）に
        配置される。これを本文より上（セクション上部）に配置したい（ユーザー要望）。
- 原因: subpage.css の @media(max-width:900px) 内で itv-qa__grid を1カラム縦積みにし、
        DOM順（本文→写真）どおり表示。さらに .itv-qa__media--first を order:0 に
        リセットしているため、写真が常に本文の下に来る。
- 修正方針: @media(max-width:900px) 内で .itv-qa__media / .itv-qa__media--first を
        order:-1 にし、写真を常に本文より上に表示する。
        ※subpage.css はReact版から無改変移植したファイルだが、ブラッシュアップ
          フェーズのため編集可。ブラッシュアップ由来の変更はコメントで明記する。
- 対象: wp-content/themes/aishin/assets/css/subpage.css

## P-005: Q&A 2つ目に写真がない→各社員に指定画像を配置（ブラッシュアップ）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番反映確認済み）
- 現象: 各インタビューのQ&A 2つ目（qa2）は写真なし仕様（seedでphoto=null）。
        ここに各社員の指定画像を配置したい（ユーザー要望）。
        佐藤: source-images/佐藤3.png / 田中: 田中1.png / 山本: 山本1.png
- 原因: seed-interviews.php で qa2 の photo を null に設定しているため。
        テンプレート（single-interview.php）は qa2_photo があれば表示するロジックの
        ため、データに画像をセットすれば表示される（テンプレ変更不要）。
- 修正方針:
        (1) 指定画像3枚をWeb用に最適化（幅1600px・JPEG）してテーマの
            assets/images/opt/ に IMG-I07(佐藤)/IMG-I08(田中)/IMG-I09(山本) として配置
        (2) seed-interviews.php の各社員 qa2 の photo に上記IDを設定＋photo_label設定
        (3) ローカルで再シード→確認、本番へ画像配置＋seed再実行
- 対象: assets/images/opt/IMG-I07..I09.jpg（新規）、seed/seed-interviews.php

## P-006: 山本のポートレート（カード・詳細FV共用）を山本7.pngに変更（ブラッシュアップ）
- 発生日: 2026-07-23
- 状態: 解決済み（2026-07-23・本番反映確認済み。カード/FV/他ページ全てIMG-Y11）
- 現象: インタビューカード（トップINTERVIEW・他インタビューの「ほかの社員も知る」）と
        山本の詳細ページFVで共用している山本のポートレート（現IMG-11）を、
        指定画像 source-images/山本7.png に変更したい（ユーザー要望）。
- 決定: カード・FVの両方を変更（ユーザー選択・portraitを一本化）。
- 修正方針: 山本7.png（334x445・縦長）をWeb用JPEG化し IMG-Y11 として opt/ に配置。
        seed-interviews.php の山本 portrait_img を IMG-11 → IMG-Y11 に変更。
        ※新IDにする理由: seed_media は既存 _aishin_img_id を再利用するため、
          IMG-11上書きだと本番メディアが差し替わらない。新IDで確実に差し替える。
        既存の幅より小さい画像だが、元画像がこの解像度のため原寸で使用。
- 対象: assets/images/opt/IMG-Y11.jpg（新規）, seed/seed-interviews.php
